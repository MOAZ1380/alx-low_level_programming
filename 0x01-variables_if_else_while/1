#include <stdio.h>
/**
 * main - moa
 * Return: moa
*/
int main(void)
{
	int i;
	int x;
	int j;

	for (i = 0; i <= 7; i++)
	{
		for (x = i + 1; x <= 8; i++)
		{
			for (j = x + 1; j <= 9; j++)
			{
				putchar('0' + i);
				putchar('0' + x);
				putchar('0' + j);
				if (!(i == 7 && x == 8 && j == 9))
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar(10);
	return (0);
}
